<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>4.1.0</version>
    </parent>
    <groupId>com.auth</groupId>
    <artifactId>api</artifactId>
    <version>0.0.1</version>
    <packaging>pom</packaging>
    <name>Auth API Manager</name>

    <properties>
        <java.version>21</java.version>
        <maven.compiler.release>21</maven.compiler.release>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>

        <!--
            Overrides de versões gerenciadas pelo spring-boot-dependencies BOM (4.1.0),
            aplicadas para corrigir vulnerabilidades reportadas pelo scan de segurança
            (Grype) até que o Spring Boot atualize essas dependências por padrão.
        -->
        <tomcat.version>11.0.25</tomcat.version>
        <log4j2.version>2.25.5</log4j2.version>
        <jackson-bom.version>3.1.5</jackson-bom.version>
        <jackson-2-bom.version>2.21.5</jackson-2-bom.version>
    </properties>

    <modules>
        <module>core</module>
        <module>framework</module>
    </modules>

</project>